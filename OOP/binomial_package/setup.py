from setuptools import setup

setup(name='distributions_pyprojects',
      version='0.1',
      description='Gaussian distributions_pyprojects',
      packages=['distributions_pyprojects'],
      zip_safe=False)